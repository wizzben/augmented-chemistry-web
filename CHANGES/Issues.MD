1. Rendering of sphere only after Reset (Button) is triggered. Should be rendered without need of reset.
2. There is a "Start AR" Button, but it should be more like a toggle, so it can be switched off as well.
3. Aspect Ratio of canvas / camera view is not fixed, should be fixed to origin webcam ratio.